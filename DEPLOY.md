# 🚀 Инструкция по деплою на GitHub Pages

## Быстрый старт

### 1. Активация GitHub Pages

1. Откройте репозиторий: https://github.com/visualGravitySense/AlbertPetenberg
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите:
   - **Source**: `GitHub Actions` (не "Deploy from a branch")
4. Сохраните изменения

### 2. Деплой изменений

Workflow автоматически запустится при push в ветку `main`:

```bash
# Если вы на другой ветке (например, ux-design-1)
git checkout main
git merge ux-design-1  # или git pull origin main

# Добавить изменения
git add .
git commit -m "Update landing page content and deploy setup"

# Запушить в main
git push origin main
```

### 3. Проверка деплоя

1. Перейдите в **Actions** в репозитории
2. Убедитесь, что workflow "Deploy to GitHub Pages" успешно выполнился
3. Сайт будет доступен через несколько минут по адресу:
   ```
   https://visualGravitySense.github.io/AlbertPetenberg/
   ```

## ✅ Что уже настроено

- ✅ GitHub Actions workflow (`.github/workflows/deploy.yml`)
- ✅ Base path в `vite.config.ts` (`/AlbertPetenberg/`)
- ✅ Build скрипт в `package.json`
- ✅ Правильные permissions для GitHub Pages

## 🔧 Ручной запуск деплоя

Если нужно запустить деплой вручную:
1. Перейдите в **Actions**
2. Выберите workflow "Deploy to GitHub Pages"
3. Нажмите **Run workflow** → **Run workflow**

## 📝 Примечания

- Первый деплой может занять 2-3 минуты
- После каждого push в `main` деплой запускается автоматически
- Если деплой не работает, проверьте настройки Pages в Settings

