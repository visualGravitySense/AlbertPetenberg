# 🚀 Инструкция по деплою на GitHub Pages

## Быстрый старт

### 1. Активация GitHub Pages

1. Откройте репозиторий: https://github.com/visualGravitySense/AlbertPetenberg
2. Перейдите в **Settings** → **Pages**
3. В разделе **Build and deployment**:
   - **Source**: выберите `GitHub Actions` (не "Deploy from a branch")
4. Сохраните изменения

⚠️ **Важно**: Если GitHub Actions не отображается как опция, сначала нужно запустить workflow хотя бы один раз.

### 2. Первый деплой

После активации GitHub Actions:

```bash
# Убедитесь, что вы на ветке main
git checkout main

# Добавить новый workflow файл и другие изменения
git add .
git commit -m "Add GitHub Pages deployment workflow"

# Запушить в main
git push origin main
```

### 3. Проверка деплоя

1. Перейдите в **Actions** в репозитории
2. Убедитесь, что workflow "Deploy to GitHub Pages" успешно выполнился
3. После успешного выполнения, вернитесь в **Settings** → **Pages**
4. Теперь должна появиться опция "GitHub Actions" - выберите её
5. Сайт будет доступен через несколько минут по адресу:
   ```
   https://visualGravitySense.github.io/AlbertPetenberg/
   ```

### 4. Последующие деплои

Workflow автоматически запустится при каждом push в ветку `main`:

```bash
git add .
git commit -m "Update content"
git push origin main
```

## ✅ Что уже настроено

- ✅ GitHub Actions workflow (`.github/workflows/deploy.yml`)
- ✅ Base path в `vite.config.ts` (`/AlbertPetenberg/`)
- ✅ Router basename в `App.tsx` (`/AlbertPetenberg`)
- ✅ Build скрипт в `package.json`
- ✅ Правильные permissions для GitHub Pages

## 🔧 Ручной запуск деплоя

Если нужно запустить деплой вручную:
1. Перейдите в **Actions** в репозитории
2. Выберите workflow "Deploy to GitHub Pages"
3. Нажмите **Run workflow** → выберите ветку `main` → **Run workflow**

## 📝 Примечания

- Первый деплой может занять 2-3 минуты
- После каждого push в `main` деплой запускается автоматически
- Если деплой не работает, проверьте:
  - Настройки Pages в Settings (должен быть выбран "GitHub Actions")
  - Логи в Actions для выявления ошибок
  - Что все файлы закоммичены и запушены

